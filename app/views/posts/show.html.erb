<h1 class="text-center">投稿詳細</h1>
<section>
  <div class="card-header">
    <h4><%= @post.title %></h4>
  </div>
  <div class="card-body">
    <p class="card-text"><%= simple_format(h @post.content)%></p>   
  </div>
  <p>タイトル <%= @post.title %></p>
  <p>内容 <%= @post.content %></p>
</section>